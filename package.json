{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "A discord bot that handles several commands listed in the commands tab",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ankur Ahir",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.6"
  }
}
